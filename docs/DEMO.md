# QuietRelay — actual-interaction film plan

Target: about 2 minutes 35 seconds, English. Every UI action below is real. This document is a plan, not a claim that a video exists. Run `npm run dev` and `npm run relay`. Open http://127.0.0.1:4326/ and restore public fixtures. Capture at 1440×1000 or similar if available. No private browser tabs or account keys.

| Time      | Actual action                                                                                      | Narration                                                                                                                                                                                                                                                                                                                        |
| --------- | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0:00–0:20 | Show workbench and selected second/location event.                                                 | A valid signature is only the beginning. This note is genuinely signed, but its tags reveal a location, a relationship and a relay destination. QuietRelay makes that boundary visible before a relay admits the message.                                                                                                        |
| 0:20–0:43 | Inspect exposure fields and signed JSON; choose Open, then Quiet.                                  | The signature stays the same. Under Open policy the note is admitted. Under Quiet it is held. It never enters the subscriber feed. We are changing a policy, not rewriting evidence to force a result.                                                                                                                           |
| 0:43–1:04 | Open Policy diff; inspect changed rows; return to Quiet.                                           | The complete fixture set runs independently under each preset. We can see the tradeoff: stricter limits hold more metadata, while rejecting otherwise authentic messages outside the contract. The choice is explicit and reproducible.                                                                                          |
| 1:04–1:30 | Sign an experiment, check content tampering, evaluate. Select original tampered row if necessary.  | These are disposable public demo identities. Signing is real BIP-340 Schnorr. Changing the content after signing breaks the canonical event hash. The relay catches that before any policy setting can admit it. Restore fixtures before the transport scene.                                                                    |
| 1:30–2:00 | Restore fixtures. Wire log → Test live relay. Show matching status and frames; scroll to snapshot. | Now the real wire. A separate loopback WebSocket relay receives EVENT and REQ messages. Its actual acknowledgements match the predicted decisions. The live subscriber receives eleven admitted events; a new subscriber retrieves seven retained records. Revisions replace old copies, and ephemeral signals are never stored. |
| 2:00–2:20 | Retained tab; inspect own deletion request and rejected foreign deletion.                          | An author can ask this local relay to withdraw their own note. Another signer cannot erase it. The deletion request remains visible. This never promises erasure from other relays or from readers who already kept a copy.                                                                                                      |
| 2:20–2:35 | Export evidence, show Method, optionally import downloaded evidence and verify.                    | Every input, arrival time, policy and decision travels in a replayable evidence file. A digest detects changes; recomputation checks the outcomes. No wallets, no upstream publication, no inference API. Understand what gets through.                                                                                          |

Three screenshot targets:

1. Default Quiet workbench with held location event and exposure panel.
2. Policy diff showing Open / Quiet / Strict outcomes.
3. Actual loopback wire exchange with “Verified against sandbox,” or retained view plus withdrawal explanation.

For a public browser deployment, the sandbox works as a static app; the optional real relay requires the local command. Do not narrate live transport on a hosted site unless it was actually run on the presenter device. If a local connection fails during capture, keep the failure truthful and use the already captured successful local sequence instead of fabricating replies.
